/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains:[
            "media.cloudbooklet.com",
            "media.istockphoto.com",
        ]
    }
}

module.exports = nextConfig
